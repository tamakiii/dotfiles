#!/bin/bash

# Seat-less on purpose: since the compositor-side fix for tamakiii/meta#1897
# (per-client seat visibility, tamakiii/meta#1898, deployed 2026-07-18) an
# ordinary client sees exactly one seat, so this reads the same clipboard
# every application uses.
wl-paste
