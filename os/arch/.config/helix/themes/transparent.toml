# Transparent OneDark theme for Helix
# Based on the built-in onedark theme but with transparent background
# This allows the terminal background to show through

inherits = "tokyonight_day"

# Make background transparent to match terminal
"ui.background" = {}

# Optional: You can also make specific UI elements transparent
# "ui.menu" = {}
# "ui.popup" = {}
# "ui.window" = {}
