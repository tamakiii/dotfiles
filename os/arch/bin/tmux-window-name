#!/bin/bash
# Output git worktree name for use in tmux automatic-rename-format
# Usage: tmux-window-name <path>

path="${1:-.}"

toplevel=$(git -C "$path" rev-parse --show-toplevel 2>/dev/null)
if [[ -n "$toplevel" ]]; then
  basename "$toplevel"
else
  basename "$path"
fi
