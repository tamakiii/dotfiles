name: CI

on: [push]

jobs:
  test:
    runs-on: macos-latest
    steps:
    - name: Checkout
      uses: actions/checkout@v1
    - run: brew --cache
    - name: Brew install
      run: make -f brew.mk
    - name: Test setup scripts
      run: make install
