name: CI

on: [push]

jobs:
  itest:
    runs-on: macos-latest
    steps:
    - name: Checkout
      uses: actions/checkout@v1
    - run: echo $SHELL
    - name: Brew install
      run: make -f brew.mk
    - name: Test setup scripts
      run: make install
