name: CI

on: [push]

jobs:
  test:
    runs-on: macos-latest
    steps:
    - name: Checkout
      uses: actions/checkout@v1
    - name: Brew config
      run: brew config
    - name: Make install
      run: |
        SKIP_BREW=1 bash etc/init/init.sh
