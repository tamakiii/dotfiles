name: CI

on: [push]

jobs:
  test:
    runs-on: macos-latest
    steps:
    - name: Checkout
      uses: actions/checkout@v1
    - name: Config
      run: brew config
    - name: make install
      run: make install
    - name: make -f brew.mk install
      run: make -f brew.mk install
